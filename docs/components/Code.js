/**
 * Compatibility wrapper for the existing MDX content.
 * Nextra renders fenced code blocks with its own copy affordance and highlighting.
 */
export function Code({ children }) {
  return children
}
